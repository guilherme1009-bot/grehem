import { Bookmark, History, Settings } from 'lucide-react'
interface NewTabPageProps { onSearch: (value: string) => void; onPanel: (panel: 'history' | 'bookmarks' | 'settings') => void; searchEngineName: string }
export function NewTabPage({ onSearch, onPanel, searchEngineName }: NewTabPageProps) {
  return <main className="new-tab-page"><section className="new-tab-content"><div className="new-tab-logo"><span className="hero-symbol">G</span><h1>Grehem</h1></div><p className="new-tab-tagline">Navegue com liberdade.</p><div className="new-tab-search"><span><SearchIcon /></span><input placeholder="Pesquise na Web ou digite um endereço" aria-label="Pesquisar na Web ou digitar endereço" onKeyDown={(event) => { if (event.key === 'Enter') onSearch(event.currentTarget.value) }} /><button onClick={(event) => { const input = event.currentTarget.parentElement?.querySelector('input'); if (input) onSearch(input.value) }}>Pesquisar</button></div><p className="search-engine-hint">Pesquisando com <strong>{searchEngineName || 'Google'}</strong></p><div className="new-tab-shortcuts"><button onClick={() => onPanel('history')}><History size={17} /><span>Histórico</span></button><button onClick={() => onPanel('bookmarks')}><Bookmark size={17} /><span>Favoritos</span></button><button onClick={() => onPanel('settings')}><Settings size={17} /><span>Configurações</span></button></div></section><footer><span>Grehem Browser</span><a href="https://sazamtecnologia.site" target="_blank" rel="noreferrer">from Sazam Tecnologia</a></footer></main>
}
function SearchIcon() { return <svg viewBox="0 0 24 24" width="19" height="19" fill="none" stroke="currentColor" strokeWidth="2"><circle cx="11" cy="11" r="7" /><path d="m20 20-3.5-3.5" /></svg> }
