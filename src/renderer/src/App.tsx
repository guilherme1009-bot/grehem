import { useEffect, useMemo, useState } from 'react'
import { ArrowLeft, ArrowRight, Bookmark, BookmarkCheck, Globe, History, Menu, Plus, RotateCw, Search, Settings, ShieldCheck, Trash2, X } from 'lucide-react'
import type { Bookmark as BookmarkRecord, BookmarkFolder, BrowserTab, HistoryEntry, SearchEngine, Setting } from '../../shared/types'
import './styles.css'

function App() {
  const [tabs, setTabs] = useState<BrowserTab[]>([]); const [activeId, setActiveId] = useState(''); const active = useMemo(() => tabs.find((item) => item.id === activeId), [tabs, activeId]); const [address, setAddress] = useState(''); const [engines, setEngines] = useState<SearchEngine[]>([]); const [settings, setSettings] = useState<Setting[]>([]); const [bookmarks, setBookmarks] = useState<BookmarkRecord[]>([]); const [folders, setFolders] = useState<BookmarkFolder[]>([]); const [history, setHistory] = useState<HistoryEntry[]>([]); const [panel, setPanel] = useState<'none' | 'history' | 'bookmarks' | 'settings'>('none'); const [historyQuery, setHistoryQuery] = useState('')
  useEffect(() => { void window.grehem.tabs.list().then(setTabs); void window.grehem.searchEngines.list().then(setEngines); void window.grehem.settings.list().then(setSettings); const offUpdate = window.grehem.onTabUpdate((tab) => setTabs((items) => items.some((item) => item.id === tab.id) ? items.map((item) => item.id === tab.id ? tab : item) : [...items, tab])); const offActive = window.grehem.onActiveTab(setActiveId); return () => { offUpdate(); offActive() } }, [])
  useEffect(() => { setAddress(active?.kind === 'newtab' ? '' : active?.url ?? '') }, [active?.id, active?.url, active?.kind])
  useEffect(() => { if (panel === 'history') void window.grehem.history.list(historyQuery).then(setHistory); if (panel === 'bookmarks') { void window.grehem.bookmarks.list().then(setBookmarks); void window.grehem.bookmarkFolders.list().then(setFolders) } }, [panel, historyQuery])
  const createTab = () => void window.grehem.tabs.create(); const submit = () => active && void window.grehem.tabs.navigate(active.id, address); const navigate = (url: string) => active && void window.grehem.tabs.navigate(active.id, url)
  const currentBookmark = bookmarks.find((item) => item.url === active?.url)
  const favorite = async () => { if (!active || active.kind === 'newtab') return; if (currentBookmark) await window.grehem.bookmarks.delete(currentBookmark.id); else await window.grehem.bookmarks.create(active.title, active.url); if (panel === 'bookmarks') setBookmarks(await window.grehem.bookmarks.list()) }
  const setting = (key: string) => settings.find((item) => item.key === key)?.value ?? ''
  const setSetting = async (key: string, value: string) => { await window.grehem.settings.set(key, value); setSettings(await window.grehem.settings.list()) }
  return <div className="app-shell"><header className="browser-chrome"><div className="tab-strip"><div className="brand-mark"><span className="brand-symbol">G</span></div><div className="tabs" role="tablist">{tabs.map((tab) => <button className={`tab ${tab.id === activeId ? 'active' : ''}`} key={tab.id} role="tab" aria-selected={tab.id === activeId} onClick={() => void window.grehem.tabs.activate(tab.id)}><span className="tab-icon">{tab.favicon ? <img src={tab.favicon} /> : <Globe size={14} />}</span><span className="tab-title">{tab.title}</span><X size={14} onClick={(event) => { event.stopPropagation(); void window.grehem.tabs.close(tab.id) }} /></button>)}</div><button className="icon-button" onClick={createTab} aria-label="Nova aba"><Plus size={18} /></button></div><div className="toolbar"><div className="navigation-actions"><button className="icon-button" disabled={!active?.canGoBack} onClick={() => active && void window.grehem.tabs.back(active.id)} aria-label="Voltar"><ArrowLeft size={18} /></button><button className="icon-button" disabled={!active?.canGoForward} onClick={() => active && void window.grehem.tabs.forward(active.id)} aria-label="Avançar"><ArrowRight size={18} /></button><button className="icon-button" onClick={() => active && void (active.isLoading ? window.grehem.tabs.stop(active.id) : window.grehem.tabs.reload(active.id))} aria-label="Recarregar"><RotateCw size={17} /></button></div><div className="address-wrap"><ShieldCheck size={16} className="security-icon" /><input value={address} onChange={(event) => setAddress(event.target.value)} onKeyDown={(event) => event.key === 'Enter' && submit()} placeholder="Pesquisar ou inserir endereço" /><button className="address-action" onClick={() => void favorite()} aria-label="Favoritar página">{currentBookmark ? <BookmarkCheck size={17} /> : <Bookmark size={17} />}</button></div><button className="icon-button" onClick={() => setPanel(panel === 'settings' ? 'none' : 'settings')} aria-label="Configurações"><Settings size={19} /></button><button className="icon-button" onClick={() => setPanel(panel === 'history' ? 'none' : 'history')} aria-label="Histórico"><History size={19} /></button></div></header>{active?.kind === 'newtab' && <main className="new-tab-page"><section className="welcome"><div className="hero-logo"><span className="hero-symbol">G</span><span>grehem</span></div><p className="tagline">Navegue com clareza.</p><div className="home-search"><Search size={19} /><input placeholder="Pesquisar na Web" onKeyDown={(event) => event.key === 'Enter' && navigate(event.currentTarget.value)} /><button onClick={() => { const input = document.querySelector<HTMLInputElement>('.home-search input'); if (input?.value) navigate(input.value) }}>Pesquisar</button></div><div className="quick-links"><button onClick={() => setPanel('bookmarks')}><Bookmark size={16} /> Favoritos</button><button onClick={() => setPanel('history')}><History size={16} /> Histórico</button><button onClick={() => setPanel('settings')}><Settings size={16} /> Configurações</button></div></section><footer><span>Grehem Browser</span><a href="https://sazamtecnologia.site" target="_blank" rel="noreferrer">from Sazam Tecnologia</a></footer></main>}{panel !== 'none' && <aside className="side-panel"><div className="panel-header"><strong>{panel === 'history' ? 'Histórico' : panel === 'bookmarks' ? 'Favoritos' : 'Configurações'}</strong><button className="icon-button" onClick={() => setPanel('none')}><X size={17} /></button></div>{panel === 'history' && <><input className="panel-search" placeholder="Pesquisar histórico" value={historyQuery} onChange={(event) => setHistoryQuery(event.target.value)} /><button className="panel-danger" onClick={async () => { await window.grehem.history.clear(); setHistory([]) }}><Trash2 size={14} /> Limpar histórico</button>{history.map((item) => <button className="list-row" key={item.id} onClick={() => { navigate(item.url); setPanel('none') }}><span>{item.title || item.url}</span><small>{item.url}</small><Trash2 size={14} onClick={(event) => { event.stopPropagation(); void window.grehem.history.delete(item.id).then(() => window.grehem.history.list(historyQuery).then(setHistory)) }} /></button>)}</>}{panel === 'bookmarks' && <>{bookmarks.map((item) => <button className="list-row" key={item.id} onClick={() => { navigate(item.url); setPanel('none') }}><span>{item.title}</span><small>{item.url}</small><Trash2 size={14} onClick={(event) => { event.stopPropagation(); void window.grehem.bookmarks.delete(item.id).then(() => window.grehem.bookmarks.list().then(setBookmarks)) }} /></button>)}<div className="panel-muted">{folders.length ? `${folders.length} pasta(s) criada(s)` : 'Pastas disponíveis quando adicionadas pela API.'}</div></>}{panel === 'settings' && <div className="settings-form"><label>Idioma<select value={setting('language')} onChange={(event) => void setSetting('language', event.target.value)}><option value="pt-BR">Português (Brasil)</option><option value="en-US">English</option><option value="es-ES">Español</option></select></label><label>Tema<select value={setting('theme')} onChange={(event) => void setSetting('theme', event.target.value)}><option value="light">Claro</option></select></label><label>Zoom<input value={setting('zoom')} onChange={(event) => void setSetting('zoom', event.target.value)} /></label><label>Mecanismo de pesquisa<select value={engines.find((item) => item.is_default)?.id ?? ''} onChange={async (event) => { await window.grehem.searchEngines.setDefault(Number(event.target.value)); setEngines(await window.grehem.searchEngines.list()) }}>{engines.map((engine) => <option key={engine.id} value={engine.id}>{engine.name}</option>)}</select></label><label>Página inicial<input value={setting('homePage')} onChange={(event) => void setSetting('homePage', event.target.value)} /></label></div>}</aside>}<div className="status-badge" aria-live="polite">{active?.isLoading && <><span className="loading-dot" /> Carregando</>}</div></div>
}
export default App
